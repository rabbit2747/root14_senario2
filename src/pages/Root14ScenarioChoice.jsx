/**
 * Root14ScenarioChoice — derekChae/1_scenario_choice 의 GatherImageHero를 그대로 렌더
 *
 * 메인 앱 안에서 직접 마운트. 별도 Vercel 프로젝트·외부 리다이렉트 X.
 */
import { GatherImageHero } from './root14-scenario/GatherImageHero';

export default function Root14ScenarioChoice() {
  return <GatherImageHero />;
}
